# Find an example for your language and learn about the core features through
# the guided tour:
# https://docs.semaphoreci.com/category/58-programming-languages
# https://docs.semaphoreci.com/category/56-guided-tour
version: v1.0
name: Hello Semaphore
agent:
  machine:
    type: e1-standard-2
  containers:
    - name: main
      image: semaphoreci/node:8-browsers
blocks:
  - name: Simple example
    task:
      env_vars:
        - name: APP_ENV
          value: prod
      jobs:
      - name: Build Code
        commands:
          - checkout
          - npm install
          - npm test
